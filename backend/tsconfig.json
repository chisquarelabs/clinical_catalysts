{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2020",                                /* Set JavaScript language version for emitted JavaScript. */
    "module": "commonjs",                              /* Specify module code generation. */
    "outDir": "./dist",                                /* Output folder for compiled files. */
    "rootDir": "./src",                                /* Root folder of your source files. */
    "strict": false,                                    /* Enable all strict type-checking options. */
    "skipLibCheck": true,                              /* Skip type checking all .d.ts files. */
    "forceConsistentCasingInFileNames": true,          /* Enforce consistent casing in file names. */

    /* Decorator and Metadata Options */
    "experimentalDecorators": true,                   /* Enable support for decorators. */
    "emitDecoratorMetadata": true,                    /* Emit type metadata for decorators. */

    /* Module Resolution */
    "esModuleInterop": true,                          /* Emit additional JS for better CommonJS interop. */
    "resolveJsonModule": true,                        /* Enable importing .json files. */
    "moduleResolution": "node",                       /* Use Node.js module resolution algorithm. */

    /* Source Maps */
    "sourceMap": true,                                /* Create source map files. */

    /* Type Checking */
    "noUnusedLocals": true,                           /* Report unused local variables. */
    "noUnusedParameters": true,                       /* Report unused function parameters. */
    "noImplicitReturns": true,                        /* Ensure all code paths in a function return. */
    "noFallthroughCasesInSwitch": true,               /* Ensure no fallthrough cases in switch statements. */

    /* Emit Options */
    "declaration": true,                              /* Generate .d.ts files for TypeScript declarations. */
    "declarationMap": false,                          /* Disable .d.ts source maps for cleaner builds. */
    "removeComments": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false                            /* Remove comments from emitted files. */
  },
  "include": ["src"],                                 /* Include all source files in the src directory. */
  "exclude": ["node_modules", "dist", "**/*.test.ts"]
   /* Exclude unnecessary files and directories. */
}
